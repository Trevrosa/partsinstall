[package]
name = "partsinstall"
version = "0.1.0"
description = "Install apps packaged in compressed parts"
edition = "2021"

[dependencies]
clap = { version = "4.5.26", features = ["derive", "env"] }
glob = "0.3.2"
